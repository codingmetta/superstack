'use client';
import AllProducts from "./subpages/AllProducts";


function Shop() {

    //    const { productCollection } = useContext(StoreContext)

    return (
        <AllProducts />
    )
}

export default Shop